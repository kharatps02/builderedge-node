extends layout
block style
  style.
    label.error {color:#FF0000 !important; font-size:13px !important; padding:0px !important; font-weight: normal !important;}
    small {margin-left: 10px;}
    .custom-bullet{padding-left: 15px;}
    .custom-bullet li {
        display: block;
    }
    .custom-bullet li:before{
        /*Using a Bootstrap glyphicon as the bullet point*/
        content: "\e080";
        font-family: 'Glyphicons Halflings';
        font-size: 9px;
        float: left;
        margin-top: 4px;
        margin-left: -17px;
        color: #CCCCCC;
    }
    .form-inline .norm-label{font-weight: normal;}
    .form-inline .submitbtn{display: block;margin-top:15px;}
    .form-inline label.error{display: block;}

block content
  body
    .container
      .page-header
        h1
          | Step3 
          small BuilderEdge Webform registration
      .row
        .col-md-12
          .panel.panel-default
            .panel-body
                //- form.form-inline(name='userInfoForm', action='/sync/initial/' + vanityKey, method='GET')
                    .form-group
                h3  Please wait while we sync the initial data. You will automatically be redirected to Salesforce once the sync is complete.
                h3  DO NOT REFRESH THE PAGE.
                img(src='/loader.gif', alt= 'loading...')
                p   Depending on the data, it may take longer. If you think there is an error please see the console by pressing F12 and contact support with the details.
                    input#appUrl.form-control(type='hidden', name='appUrl' value=appUrl)
                    //- button.btn.btn-primary.submitbtn(type='submit') Done!
    //- script(src='/common/js/jquery-3.1.0.min.js')
    script(src='http://code.jquery.com/jquery-3.3.1.min.js')

    //- script(src='/common/js/bootstrap.min.js')
    script(src='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js')
    script(src='/data-sync.js')
    //- script(src='/common/js/jquery.validate.min.js')
    script(src='https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.17.0/jquery.validate.min.js')
    //- script(src='/common/js/registrationPageValidation.js?v=1.0')