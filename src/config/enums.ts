export namespace Enums {
    export enum RESPONSE_STATUS {
        SUCCESS = 'SUCCESS',
        ERROR = 'ERROR',
    }
    export enum COMETD_LOG_MODES {
        ERROR = 'error',
        INFO = 'info',
        WARNING = 'warn',
        DEBUG = 'debug',
    }
}
